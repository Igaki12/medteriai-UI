# medteriai-UI

GitHub Pages で公開する UI 専用デモです。実際の API は存在せず、画面上の挙動はダミーで再現しています。

## 開発手順
1. `npm install`
2. `npm run dev`

## ビルド（GitHub Pages /docs）
1. `npm run build`
2. 生成された `docs/` を GitHub Pages の公開ディレクトリに設定

## 補足
- 生成処理は **10秒〜2分のランダム**で完了したように見せています。
- PDF のアップロード／ダウンロードもダミー動作です。
